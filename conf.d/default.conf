server {
    listen       80;
    server_name  localhost;
    absolute_redirect off;

    # Block direct access to Cloud Run URL (must go through Cloudflare Tunnel)
    if ($http_cf_connecting_ip = "") {
        return 403;
    }

    location /images/ {
        root /usr/share/nginx/html;
        add_header Cache-Control "public, max-age=60, stale-while-revalidate=3600";
        add_header CDN-Cache-Control "max-age=300, stale-while-revalidate=3600";
    }

    location /thumbnails/ {
        root /usr/share/nginx/html;
        add_header Cache-Control "public, max-age=60, stale-while-revalidate=3600";
        add_header CDN-Cache-Control "max-age=300, stale-while-revalidate=3600";
    }

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "public, max-age=60, stale-while-revalidate=3600";
        add_header CDN-Cache-Control "max-age=300, stale-while-revalidate=3600";
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
        add_header Cache-Control "public, max-age=60, stale-while-revalidate=3600";
        add_header CDN-Cache-Control "max-age=300, stale-while-revalidate=3600";
    }
}
