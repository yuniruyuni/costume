// yunirun のデプロイ宣言。
//
// ここに書くのは「アプリだけが知っていること」に限る。ホスト側のポートや uid、
// DB 名やロール名は yunirun が導出するので現れない。
{
  "app": {
    // yuniruyuni.net 側のアプリ名は costume2 (旧 apps.nix 側と並行して動かすため)
    // だが、push している image は costume。移行が済んだら名前が揃う。
    "image": "costume"
  }
}
